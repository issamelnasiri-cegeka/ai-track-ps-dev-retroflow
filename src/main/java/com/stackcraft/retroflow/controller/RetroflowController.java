package com.stackcraft.retroflow.controller;

import org.springframework.web.bind.annotation.RestController;

/**
 * STUB — left behind by the previous vendor. No endpoints implemented yet.
 *
 * See the RetroFlow brief for the required operations: creating and
 * retrieving teams, creating/closing retrospectives, adding feedback
 * items, marking action items complete, and filtering action items by
 * priority and completed status.
 */
@RestController
public class RetroflowController {
}
